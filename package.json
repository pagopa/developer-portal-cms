{
  "private": true,
  "workspaces": [
    "apps/strapi-cms",
    "packages/*"
  ],
  "scripts": {
    "clean": "turbo run clean",
    "compile": "turbo run compile",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "changeset": "changeset",
    "version": "changeset version && npm i",
    "tag": "changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "^1.13.4"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "elliptic": "^6.6.1",
    "koa": "^3.0.1"
  }
}
